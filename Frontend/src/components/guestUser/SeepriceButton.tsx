import { Button } from "@/components/ui/button"

function SeepriceButton() {

    return (
        <div className="fixed bottom-0 z-555 bg-white p-4 w-full">
            <Button className="bg-black text-white py-[26px] rounded-lg w-full text-lg font-semibold">
                See price
            </Button>
        </div>

    )
}

export default SeepriceButton
